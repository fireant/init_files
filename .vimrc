if has('mouse')
  set mouse=a
endif

set laststatus=2 " always show status line

set number " show line number column

set cursorline " highlight the current line

set so=5 " five lines as scroll offset

